# T2 annotator: RNApolis (annotator emits JSON). Selected when 'rnapolis' is in --annotators.
name: ws1-rnapolis
channels:
  - conda-forge
  - bioconda
dependencies:
  - python>=3.10
  - pip
  - pip:
      - rnapolis==0.16.7
