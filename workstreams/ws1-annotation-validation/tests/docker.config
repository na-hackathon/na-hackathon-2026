// Per-test overlay used by the docker-mode pipeline tests.
// Picks the tzok/maxit container path in convert.nf, leaves conda enabled
// so the python tools (rnapolis, gemmi, forgi) still resolve.
docker.enabled = true
