// nf-test runtime config: disable conda so the local python env is used,
// matching how `nextflow run -profile test` already runs the skeleton.
conda.enabled = false
docker.enabled = false
singularity.enabled = false
