# T2 annotator FR3D — deps for the pinned fr3d-python submodule (run via PYTHONPATH; see modules/annotate.nf).
name: ws1-fr3d
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pip
  - pip:
      - numpy
      - scipy
      - mmcif-pdbx
